# KDBX Interop Corpus (CORE-04)

All files are **synthetic** (script-generated, no real secrets). Uniform
database password: `correct-horse-battery`. Regenerate with
`scripts/generate-corpus.sh` (requires local keepassxc-cli + Swift toolchain).

| File | Producer | Coverage | Password material |
|------|----------|----------|-------------------|
| kxc-default.kdbx | KeePassXC | Argon2id defaults, AES-CBC, gzip, url/notes fields | password |
| kxc-history.kdbx | KeePassXC | entry history (2 revisions, notes+password rotations) | password |
| kxc-recyclebin.kdbx | KeePassXC | recycle bin group + removed entry | password |
| kxc-attachment.kdbx | KeePassXC | 64 KiB binary attachment | password |
| kxc-keyfile.kdbx (+ .keyx) | KeePassXC | password + key file (XML v2.0) combination | password + keyx |
| rv-argon2d.kdbx | RavenCore | Argon2d KDF (verified by keepassxc-cli) | password |
| rv-aeskdf.kdbx | RavenCore | AES-KDF 600k (verified by keepassxc-cli) | password |
| rv-chacha20.kdbx | RavenCore | ChaCha20 outer cipher (verified by keepassxc-cli) | password |
| rv-nocompress.kdbx | RavenCore | compression disabled (verified by keepassxc-cli) | password |
| kxc-passkey.kdbx | KeePassXC 2.7.12 (one-time artifact) | passkey entry: 7 KPEX_PASSKEY_* attrs, secrets protected (regression lock, CorpusGateTests) | password |
| rv-passkey.kdbx | RavenCore | KdbxPasskey.write layout, per-attribute keepassxc-cli verification | password |

## Passkey fixtures (shared synthetic credential, 02-01)

Both passkey fixtures carry the SAME fixed synthetic credential (no real
secrets; the PEM below is a one-time disposable P-256 PKCS#8 key):

- rpName / Title: `RavenTest` / `RavenTest (Passkey)`
- rpID (KPEX_PASSKEY_RELYING_PARTY): `example.com`
- username (KPEX_PASSKEY_USERNAME): `alice@example.com`
- URL: `https://example.com/login`
- credentialID (KPEX_PASSKEY_CREDENTIAL_ID): `-cLXmjDNY0U6pHYpDx7FbENwTKlX95xzV5FQQh0U-38`
  (= base64url of SHA-256("ravenvault-fixture-credential"))
- userHandle (KPEX_PASSKEY_USER_HANDLE): `ngHFbgaTCoa06Mo5sdcLr5cIR_jDSx8WoDsSe7eI9f8`
  (= base64url of SHA-256("ravenvault-fixture-user"))
- KPEX_PASSKEY_FLAG_BE = `1`, KPEX_PASSKEY_FLAG_BS = `0` (deliberately different)
- KPEX_PASSKEY_PRIVATE_KEY_PEM (PKCS#8):

      -----BEGIN PRIVATE KEY-----
      MIGHAgEAMBMGByqGSM49AgEGCCqGSM49AwEHBG0wawIBAQQgM2NYELpT/P9sW3kV
      qFIMtWUvVIyWyTXMVBj/96uxyYuhRANCAARmIvPFfIOMtnSQmsDXpRcD6ifYB3X6
      ZfRM1QkysuHcAwQ5je4rtYTI7S4vH092dm+MfMCg+c50/LFQLUAXYzGv
      -----END PRIVATE KEY-----

Provenance: `rv-passkey.kdbx` is regenerated by this script (RavenCore write
+ keepassxc-cli verification). `kxc-passkey.kdbx` is committed once and only
stash/restored here (protected custom attributes cannot be authored by
keepassxc-cli); see scripts/CROSS-VALIDATION.md for its production record.

## Failure-driven growth (D-11)

Any interop bug found in the wild: add a sanitized sample here as a new
fixture + its expected-value row in `CorpusGateTests`, and the gate grows a
permanent regression. The gate NEVER silently passes on an empty directory.
